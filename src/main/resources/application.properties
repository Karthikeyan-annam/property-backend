# Use Railway provided URL (convert mysql:// → jdbc:mysql://)
spring.datasource.url=mysql://root:WYRZhKpirIddKzCypJgKJIXbOexkvibZ@mysql.railway.internal:3306/railway

# These are NOT needed if MYSQL_URL is used, but safe fallback:
spring.datasource.username=root
spring.datasource.password=WYRZhKpirIddKzCypJgKJIXbOexkvibZ

# Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server port (Railway assigns PORT automatically)
server.port=${PORT:8080}
